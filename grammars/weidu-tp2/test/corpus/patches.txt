================================================================================
SAY patch
================================================================================

COPY ~mymod/item.itm~ ~override~
BEGIN
  SAY 0x08 ~New Name~
END

--------------------------------------------------------------------------------

(source_file
  (action_copy
    (file_pair
      from: (string
        (tilde_string
          (tilde_content)))
      to: (string
        (tilde_string
          (tilde_content))))
    (patch_block
      (patch_say
        offset: (number)
        text: (string
          (tilde_string
            (tilde_content)))))))

================================================================================
PATCH_PRINT
================================================================================

COPY ~mymod/item.itm~ ~override~
BEGIN
  PATCH_PRINT ~Processing file~
END

--------------------------------------------------------------------------------

(source_file
  (action_copy
    (file_pair
      from: (string
        (tilde_string
          (tilde_content)))
      to: (string
        (tilde_string
          (tilde_content))))
    (patch_block
      (patch_print
        message: (string
          (tilde_string
            (tilde_content)))))))

================================================================================
TEXT_SPRINT
================================================================================

COPY ~mymod/item.itm~ ~override~
BEGIN
  TEXT_SPRINT myvar ~hello~
END

--------------------------------------------------------------------------------

(source_file
  (action_copy
    (file_pair
      from: (string
        (tilde_string
          (tilde_content)))
      to: (string
        (tilde_string
          (tilde_content))))
    (patch_block
      (patch_text_sprint
        var: (variable_ref
          (identifier))
        value: (string
          (tilde_string
            (tilde_content)))))))

================================================================================
REPLACE_TEXTUALLY
================================================================================

COPY ~mymod/script.baf~ ~override~
BEGIN
  REPLACE_TEXTUALLY ~oldtext~ ~newtext~
END

--------------------------------------------------------------------------------

(source_file
  (action_copy
    (file_pair
      from: (string
        (tilde_string
          (tilde_content)))
      to: (string
        (tilde_string
          (tilde_content))))
    (patch_block
      (patch_replace_textually
        regexp: (string
          (tilde_string
            (tilde_content)))
        replacement: (string
          (tilde_string
            (tilde_content)))))))

================================================================================
EVALUATE_BUFFER
================================================================================

COPY ~mymod/script.baf~ ~override~
BEGIN
  EVALUATE_BUFFER
END

--------------------------------------------------------------------------------

(source_file
  (action_copy
    (file_pair
      from: (string
        (tilde_string
          (tilde_content)))
      to: (string
        (tilde_string
          (tilde_content))))
    (patch_block
      (patch_evaluate_buffer))))

================================================================================
TO_UPPER and TO_LOWER
================================================================================

COPY ~mymod/item.itm~ ~override~
BEGIN
  TO_UPPER myvar
  TO_LOWER othervar
END

--------------------------------------------------------------------------------

(source_file
  (action_copy
    (file_pair
      from: (string
        (tilde_string
          (tilde_content)))
      to: (string
        (tilde_string
          (tilde_content))))
    (patch_block
      (patch_to_upper
        var: (identifier))
      (patch_to_lower
        var: (identifier)))))
