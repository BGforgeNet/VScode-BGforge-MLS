// Minimal type stub for web-tree-sitter's EmscriptenModule reference.
// web-tree-sitter's type definitions reference EmscriptenModule but don't declare it.
declare global {
    // eslint-disable-next-line no-unused-vars -- global type used by web-tree-sitter, not our code
    interface EmscriptenModule {}
}
export {};
