/** Runtime wrapper around the shared tooltip table rendering module. */
export {
    buildFalloutArgsTable,
    buildWeiduTable,
    type VarRow,
    type VarSection,
} from "../../../shared/tooltip-table";
